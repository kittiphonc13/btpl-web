# Product Requirements Document (PRD): Blood Pressure Logging Web Application

**Version:** 1.1
**Date:** 31 กรกฎาคม 2568

---

## 1. ภาพรวม (Overview) 📝

เอกสารนี้จัดทำขึ้นเพื่อกำหนดขอบเขตและรายละเอียดของ Web Application สำหรับการบันทึกข้อมูลความดันโลหิตส่วนบุคคล โดยมีเป้าหมายเพื่อให้ผู้ใช้งาน (User) สามารถจัดการข้อมูลสุขภาพของตนเองได้อย่างสะดวก, ติดตามประวัติการใช้ยา และดูแนวโน้มของค่าความดันโลหิตได้ง่ายยิ่งขึ้น

---

## 2. เทคโนโลยีที่ใช้ (Tech Stack) 💻

- **Frontend:** Next.js
- **Backend:** FastAPI (Python)
- **Database:** Supabase

**ข้อกำหนดเพิ่มเติม:**
- Library, Framework และ Dependencies ทั้งหมดที่ใช้ในโปรเจกต์จะต้องเป็น **Version ล่าสุด (Latest Stable Version)** ณ วันที่พัฒนา
- สถาปัตยกรรมของระบบจะต้องแยกส่วนระหว่าง Frontend และ Backend ออกจากกันอย่างชัดเจน

---

## 3. คุณสมบัติหลัก (Core Features) ✨

### 3.1. ระบบสมาชิกและการยืนยันตัวตน (User Authentication)

- **User Registration:**
    - ผู้ใช้สามารถลงทะเบียนเพื่อสร้างบัญชีใหม่โดยใช้ **Email** และกำหนด **Password** ของตนเอง
    - การลงทะเบียนสำเร็จจะสร้าง Account ในระบบ Authentication ของ Supabase
- **User Login:**
    - ผู้ใช้สามารถเข้าสู่ระบบด้วย Email และ Password ที่ลงทะเบียนไว้
- **Data Privacy:**
    - ผู้ใช้แต่ละคนจะสามารถเข้าถึง, แก้ไข และดูข้อมูลได้เฉพาะของตนเองเท่านั้น

### 3.2. การจัดการโปรไฟล์ผู้ใช้ (User Profile Management)

- ผู้ใช้สามารถสร้างและแก้ไขข้อมูลส่วนตัวของตนเองได้ โดยมีรายละเอียดที่ต้องจัดเก็บดังนี้
    - `user_id`: UUID (Primary Key, Foreign Key จาก `auth.users` ของ Supabase)
    - `full_name`: ชื่อ-สกุล (Text)
    - `nickname`: ชื่อเล่น (Text, Optional)
    - `date_of_birth`: วันเดือนปีเกิด (Date)
        - **UI Requirement:** ให้เลือกจากปฏิทิน (Calendar Picker)
        - **Format:** จัดเก็บในรูปแบบที่สามารถคำนวณอายุได้ (แนะนำเป็น ISO 8601) และแสดงผลเป็นปีพุทธศักราช
    - `medical_conditions`: โรคประจำตัว (Text Area)
    - `gender`: เพศ (Dropdown: ชาย, หญิง, อื่นๆ) (Suggested)
    - `created_at`: วันที่สร้างโปรไฟล์ (Timestamp)
    - `updated_at`: วันที่แก้ไขโปรไฟล์ล่าสุด (Timestamp)

### 3.3. การบันทึกยาที่รับประทาน (Medication Log)

- ผู้ใช้สามารถเพิ่ม, แก้ไข และลบรายการยาที่รับประทานเป็นประจำได้
    - `medication_id`: UUID (Primary Key)
    - `user_id`: UUID (Foreign Key)
    - `medicine_name`: ชื่อยาหรือชื่อตัวยา (Text)
    - `dosage_mg`: ขนาดของยา (Integer, หน่วยเป็นมิลลิกรัม)
    - `quantity`: ปริมาณที่ต้องรับประทาน (Text, เช่น 1 เม็ด, 1 ช้อนชา)
    - `intake_time`: **เวลารับประทาน (รองรับการเลือกหลายค่า)**
        - **UI Requirement:** ให้เลือกจาก **Multiple Selection** (เช่น Checkboxes หรือ Multi-select dropdown)
        - **Data Storage:** ควรจัดเก็บในรูปแบบ **Array of Text**
        - **ตัวเลือก:** ก่อนอาหารเช้า, หลังอาหารเช้า, ก่อนอาหารกลางวัน, หลังอาหารกลางวัน, ก่อนอาหารเย็น, หลังอาหารเย็น, ก่อนนอน, อื่นๆ (ระบุเอง)
    - `is_active`: สถานะการใช้งาน (Boolean, เพื่อระบุว่ายังทานยานี้อยู่หรือไม่) (Suggested)
    - `notes`: หมายเหตุเพิ่มเติม (Text Area)
    - `created_at` / `updated_at`: Timestamps

### 3.4. การบันทึกค่าความดันโลหิต (Blood Pressure Log)

- ผู้ใช้สามารถบันทึกค่าความดันโลหิตในแต่ละวันได้
    - `record_id`: UUID (Primary Key)
    - `user_id`: UUID (Foreign Key)
    - `record_datetime`: วันและเวลาที่วัดค่า (Timestamp)
        - **UI Requirement:** ให้เลือก **วันเดือนปี (พ.ศ.)** จาก Calendar Picker และ **เวลา (รูปแบบ 24 ชั่วโมง)** จาก Time Picker แยกกัน แล้วนำมารวมกันก่อนบันทึก
    - `systolic`: ค่าความดันตัวบน (Integer)
    - `diastolic`: ค่าความดันตัวล่าง (Integer)
    - `heart_rate`: อัตราการเต้นของหัวใจ (Integer)
    - `notes`: หมายเหตุเพิ่มเติม (Text Area, เช่น "รู้สึกเวียนหัวเล็กน้อย")
    - `created_at`: วันที่สร้างรายการบันทึก (Timestamp, เพื่อการเรียงลำดับข้อมูล)

---

## 4. หน้า Homepage (Dashboard) 📊

- หลังจากผู้ใช้ Login เข้าสู่ระบบ จะพบกับหน้า Homepage ซึ่งเป็น Dashboard หลัก
- **แสดงรายการค่าความดันโลหิต:**
    - แสดงข้อมูลที่ผู้ใช้บันทึกไว้ **จำนวน 25 รายการล่าสุด** ในรูปแบบตารางหรือการ์ดที่อ่านง่าย
    - **การเรียงลำดับ:** เรียงจากรายการที่ **บันทึกล่าสุดไปหารายการเก่าสุด** (Descending by `created_at`)
- **Export Data:**
    - มีปุ่ม **"Export to Excel"** ที่ชัดเจน
    - เมื่อกดปุ่ม ระบบจะทำการ Export ข้อมูลค่าความดันโลหิต **ทั้งหมด** ของผู้ใช้ออกมาเป็นไฟล์ `.xlsx` เพียงรูปแบบเดียวเท่านั้น

---

## 5. ข้อกำหนดอื่นๆ (Non-Functional Requirements) ⚙️

- **Code Quality:**
    - โค้ดต้องมีการจัดระเบียบ, อ่านเข้าใจง่าย, มีการเขียน Comment ในส่วนที่ซับซ้อน และง่ายต่อการบำรุงรักษาและต่อยอดในอนาคต
- **API Design:**
    - Backend API ควรออกแบบตามหลักการ RESTful หรือ GraphQL ที่มีความชัดเจนและสอดคล้องกันตลอดทั้งโปรเจกต์
- **Usability:**
    - UI/UX ควรออกแบบมาให้ใช้งานง่าย (User-Friendly) และรองรับการใช้งานบนอุปกรณ์ต่างๆ (Responsive Design)